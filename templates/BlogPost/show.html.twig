{% extends 'base.html.twig' %}
{% block body %}
    <div class="row">
        <h1 class="col">{{ entity.game.name }}: {{ entity.title }}</h1>
        <div class="col text-right">
            {% trans from "messages" %}posted_at{% endtrans %} {{ entity.createdAt|date("d.m.Y H:i") }} <br/>
            {% trans from "messages" %}updated_at{% endtrans %} {{ entity.modifiedAt|date("d.m.Y H:i") }}
        </div>
    </div>
    <hr/>
    <div class="row">
        <div class="col">
            {{ entity.content| raw }}
        </div>
    </div>
{% endblock %}
